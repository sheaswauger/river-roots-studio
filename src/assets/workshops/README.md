# Workshop Hero Images

Place hero images here named by slug, e.g. `repurposing-wonder.jpeg`.

The schema marks `heroImage` as optional. Workshops without an image render
with a CSS gradient placeholder using brand colors.
